# Requires the following python libraries:
- `flask`
- `easyocr`
- `indic_transliteration`

# To run the file:
## start the backend:
`python app.py`

## start the frontend
`python -m http.server 8000`
